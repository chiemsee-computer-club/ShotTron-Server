{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AppConfig": {
    "JwtAudience": "${JWT_AUDIENCE}",
    "JwtIssuer": "${JWT_ISSUER}",
    "JwtSecret": "${JWT_SECRET}"
  }
}
